"""Archived env configs -- not registered, not actively maintained.

Nothing under experiments/envs actively imports from here (see the top-level
experiments/envs/__init__.py registration list), so nothing here shows up as an
--env.type choice. Files are kept for reference/history; import them directly by path if you
need to run one again.
"""
